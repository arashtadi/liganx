## Liganx site monitor — 2026-05-03 (automated run)

### 🔴 CRITICAL — needs immediate attention
- None

### 🟡 SHOULD-FIX — non-blocking but real
- None

### 🟢 HEALTHY — these are working
- Backend `/health`: status=ok, env=production, git_sha=d50d8fd (1 commit behind origin/main; sole divergent commit is frontend-only — deploy lag expected, not stalled)
- All 4 frontend pages (`/`, `/validation`, `/library`, `/contact`) return 200 in <330 ms
- Validation snapshot fresh (timestamp 2026-05-02T06:22:44Z, 1 day old) — all 8 verdicts consistent with 3-branch logic (resistance / selectivity / retained)
- `verify_prep_symmetry.py` PASS — WT skips minimisation, mutant minimises (asymmetric invariant intact)
- `verify_catalog.py` PASS — 13 targets, 40 mutations, all pocket centroids within tolerance or exempted
- Cost gates intact: `BOLTZ2_ENABLED=false` (Boltz-2 pod still paused, ~$497/mo savings); `GNINA_ENABLED=1`; `QUICK_DOCK_ENABLED=true`
- Science spot-check clean: T315I, V600E, H1047R mutation hints all accurate

### 📊 Snapshot stats
- Backend: git_sha=d50d8fd, response=<400 ms
- Validation: 5/8 PASS, 3/8 NOISE, 0/8 FAIL, snapshot age=1d
- Open issues from previous run: 0 (yesterday was all-healthy)

---

## Phase-by-phase detail

### Phase 1 — Backend health
```
{"status":"ok","version":"0.0.1","env":"production","git_sha":"d50d8fd"}
```
`git rev-parse --short origin/main` = `36c14c2` — backend is 1 commit behind.
`git log --name-only d50d8fd..origin/main -- backend/ pipeline/ Dockerfile fly.toml` → **no output**.
The sole divergent commit (`36c14c2 Step 3 re-edit save: surface 'Update <name>' as primary action`) touches frontend-only paths. Fly auto-deploy correctly skipped — **HEALTHY, not a stalled deploy**.

### Phase 2 — Frontend smoke
| URL | Status | Time |
|---|---|---|
| https://liganx.com/ | 200 | 0.314 s |
| https://liganx.com/validation | 200 | 0.328 s |
| https://liganx.com/library | 200 | 0.312 s |
| https://liganx.com/contact | 200 | 0.310 s |

All pages fast and healthy.

### Phase 3 — Validation snapshot freshness + verdict consistency
- `timestamp_utc` = 2026-05-02T06:22:44Z (age **1 day**)
- `noise_floor_kcal` = 1.0
- `summary` = {total:8, pass:5, fail:0, noise:3, skip:0} — counts sum correctly

Per-case verdict consistency (3-branch logic including `retained`):

| Case | expected_direction | Δ kcal/mol | verdict | consistent |
|---|---|---|---|---|
| ABL T315I — Imatinib | resistance | +1.20 | PASS | ✓ |
| EGFR T790M — Gefitinib | resistance | +1.30 | PASS | ✓ |
| EGFR T790M — Osimertinib | selectivity | +0.20 | NOISE | ✓ |
| BRAF V600E — Vemurafenib | selectivity | 0.00 | NOISE | ✓ |
| KIT D816V — Imatinib | resistance | +3.00 | PASS | ✓ |
| KIT D816V — Avapritinib | selectivity | −0.40 | NOISE | ✓ |
| BTK C481S — Ibrutinib | resistance | +1.80 | PASS | ✓ |
| BTK C481S — Pirtobrutinib | **retained** | +0.50 | PASS | ✓ (|Δ|=0.50 ≤ noise floor — correct retention result) |

0 inconsistencies.

### Phase 4 — Science spot check (3 mutation hints)
Spot-checked from `frontend/src/components/KetcherModal.tsx` MUTATION_HINTS:

- **T315I** — "Gatekeeper Thr→Ile loses an H-bond donor and adds steric bulk. Linkers that bypass the gatekeeper region often survive." ✓ Accurate — the Thr315 hydroxyl H-bond to inhibitor NH and steric clash with Ile are the canonical ABL resistance mechanisms; Ponatinib's bypass linker is the clinical example.
- **V600E** — "Adds a hydrophobic gain near residue 600. Methyl, ethyl, or fluoro at meta/para of an aryl group often fills the new pocket." ✓ Defensible — "hydrophobic gain" refers to the DFG-in activation-pocket conformation V600E locks in, not the Glu residue itself; this nuance is captured in the adjacent code comment.
- **H1047R** — Allosteric caveat ("rigid-receptor docking has limited predictive power here") correctly present after medchem-phd v2 audit. ✓

No corrections needed.

### Phase 5 — Recent-deploy regression
Last 10 commits on `origin/main`:
```
36c14c2  Step 3 re-edit save: surface 'Update <name>' as primary action
d50d8fd  Better Optimize variants: mutation-aware design + Pod pre-flight + clean errors
566f41d  Fix admin panel 500: SQLAlchemy parsed ':uid' inside SQL comment as bind param
953a385  AI diagnose mode for invalid SMILES
7c94ff7  Editor rail header: split into title + prominent status pills
643fb42  Real-time SMILES validity dot + SA Score chip in editor
7cde8ee  fix(quick_dock): pre-flight rejects molecules vina-gpu would crash on + Liganx AI label
334f803  ux(editor): disable Re-dock when score is fresh for live SMILES
ac805c4  fix(admin): surface /admin/users 503 root cause + per-row defensiveness
175daf7  fix(newjob): persist sketcher-renamed compounds to user library
```
- `KetcherModal.tsx` touched in recent commits — MUTATION_HINTS map well-formed, 11 entries, no malformed entries
- `verify_prep_symmetry.py` PASS (asymmetric WT-not-minimised invariant intact)
- `verify_catalog.py` PASS (13 targets, all exemptions accounted for)
- No PREP_VERSION bump in last 10 commits — no cache-flush latency expected
- Backend deploy lag: 1 commit ahead (frontend-only) → expected, HEALTHY

### Phase 6 — Cost & infra sanity
```
BOLTZ2_ENABLED=false   ✓ — Boltz-2 pod paused (~$497/mo savings intact)
GNINA_ENABLED=1        ✓
QUICK_DOCK_ENABLED=true ✓
```
No drift detected.

---

## Comparison with previous run (2026-05-02)
Previous run: all-healthy, no CRITICAL, no SHOULD-FIX.
This run: all-healthy, no CRITICAL, no SHOULD-FIX.
**No severity escalation triggered** (no finding has persisted 3+ runs; this is run 2).

## Telegram action
All-healthy → **no Telegram post** (quiet-by-default policy).
Today is Sunday 2026-05-03 → not Monday (no weekly digest) and not 1st of month.
